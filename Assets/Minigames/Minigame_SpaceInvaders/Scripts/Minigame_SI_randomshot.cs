using UnityEngine;
using System.Collections;

public class Minigame_SI_randomshot : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    /*
    Transform RandomInvader() {                 // soll einen RandomInvader zurück geben damit dieser nach einen bestimmten Cooldown schießt(in Update)
        var allInvaders = GameObject.FindGameObjectsWithTag("Gegner") < Transform >;
		if (allInvaders.Length == 0) {
			return null;
		} else {
			return allInvaders[Random.Range(0, allInvaders.Length-1)];
		}
	}*/
}
